# EARNED-MEMORY-001

Prerequisite integration proof for governed recursive improvement.

The protocol is frozen against `openline-reports` main commit `fd5fc4ba3a0f23d69383769d2eac3b5dea04ca60` before the primary CI result.

Run only through normal PR/push CI first. A green job is not by itself the scientific verdict; inspect the new earned-memory step and independently verified output before freezing a result.

No `RESULT.json` is checked in yet. Do not create one until the prospective run completes and its semantic result is inspected.
