# Identity Binding swarm contract

The research swarm may propose an entity mapping, attach discriminating evidence, and surface contradictions. It does not decide whether the mapping is accepted. `authority` is fixed to `NONE`, and no proposal field can force COMMIT, DENY, or QUARANTINE. A receiver-owned Identity Binding Gate consumes the proposal separately.
